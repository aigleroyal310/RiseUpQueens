/* =====================================================
   CONFIG SUPABASE — MODÈLE
   -----------------------------------------------------
   Copie ce fichier en `supabase-config.js` (gitignoré)
   et remplis tes valeurs. NE COMMITE PAS supabase-config.js.

   La "publishable key" (sb_publishable_...) est PUBLIQUE par
   conception : elle est envoyée au navigateur. Ce n'est pas
   elle qui protège tes données — c'est la RLS (voir supabase/schema.sql).
   Ne mets JAMAIS ici une "secret key" (sb_secret_...).
   ===================================================== */
window.SUPABASE_URL = 'https://TON-PROJET.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_XXXXXXXXXXXXXXXXXXXXXXXX';
